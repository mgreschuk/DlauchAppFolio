---
phase: 01-foundation-appfolio-connectivity
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(authenticated)/layout.tsx
  - src/app/(authenticated)/dashboard/page.tsx
  - src/app/(authenticated)/unit-turn/page.tsx
  - src/components/sidebar.tsx
  - src/components/sidebar-nav.tsx
  - src/components/logout-button.tsx
  - src/lib/auth-helpers.ts
autonomous: true
requirements:
  - PORTAL-01
  - PORTAL-04

must_haves:
  truths:
    - "Authenticated users see a sidebar with product name, Dashboard nav item, and logout"
    - "Dashboard nav item is active when on /dashboard (accent left border)"
    - "Dashboard page shows page heading 'Dashboard'"
    - "Unit Turn nav card on dashboard links to /unit-turn placeholder page"
    - "Logout button in sidebar footer immediately signs out the user"
    - "Sidebar is 240px wide with secondary background"
  artifacts:
    - path: "src/app/(authenticated)/layout.tsx"
      provides: "Authenticated layout with sidebar"
      contains: "sidebar"
    - path: "src/app/(authenticated)/dashboard/page.tsx"
      provides: "Dashboard home page"
      contains: "Dashboard"
    - path: "src/components/sidebar.tsx"
      provides: "Sidebar component with nav and footer"
      contains: "Turnkey"
    - path: "src/components/logout-button.tsx"
      provides: "Logout button using Auth.js signOut"
      contains: "signOut"
  key_links:
    - from: "src/app/(authenticated)/layout.tsx"
      to: "src/components/sidebar.tsx"
      via: "import and render"
      pattern: "Sidebar"
    - from: "src/components/logout-button.tsx"
      to: "src/auth.ts"
      via: "signOut server action or next-auth/react"
      pattern: "signOut"
---

<objective>
Build the authenticated portal shell: sidebar navigation, dashboard home page, and Unit Turn placeholder page.

Purpose: Establishes the portal layout that every future phase builds inside. Users can navigate the app and see the dashboard after login.
Output: Working portal shell with sidebar, dashboard, and Unit Turn placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-appfolio-connectivity/01-CONTEXT.md
@.planning/phases/01-foundation-appfolio-connectivity/01-UI-SPEC.md
@.planning/phases/01-foundation-appfolio-connectivity/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sidebar component and authenticated layout</name>
  <files>
    src/components/sidebar.tsx, src/components/sidebar-nav.tsx,
    src/components/logout-button.tsx, src/lib/auth-helpers.ts,
    src/app/(authenticated)/layout.tsx
  </files>
  <read_first>
    src/auth.ts
    src/app/layout.tsx
    .planning/phases/01-foundation-appfolio-connectivity/01-UI-SPEC.md
    src/components/ui/button.tsx
    src/components/ui/separator.tsx
  </read_first>
  <action>
1. Create `src/lib/auth-helpers.ts` — server-side session helper:
   ```typescript
   import { auth } from "@/auth";
   import { redirect } from "next/navigation";

   export async function requireAuth() {
     const session = await auth();
     if (!session?.user) redirect("/login");
     return session;
   }
   ```

2. Create `src/components/logout-button.tsx` — Client component:
   - Uses `signOut` from "next-auth/react"
   - Renders shadcn Button with variant="ghost" and destructive color (#ef4444)
   - Text: "Log Out" per UI-SPEC
   - On click: `signOut({ callbackUrl: "/login" })`
   - Hover state: slate-700 background
   - Focus: 2px offset ring
   - No confirmation dialog per UI-SPEC

3. Create `src/components/sidebar-nav.tsx` — Client component for nav items:
   - Uses `usePathname()` from "next/navigation" to determine active route
   - Renders nav items as Next.js `Link` components
   - Active item: 4px solid accent (#3b82f6) left border, label in semibold (600)
   - Inactive item: transparent background, muted text (#94a3b8)
   - Hover: slate-700 background (#334155)
   - Focus: 2px offset ring
   - Minimum touch target height: 44px per UI-SPEC accessibility floor
   - Nav padding: 16px (md spacing token)
   - Phase 1 nav items array:
     ```typescript
     const navItems = [
       { label: "Dashboard", href: "/dashboard", icon: LayoutDashboard },
     ];
     ```
   - Icon from lucide-react: `LayoutDashboard` for Dashboard

4. Create `src/components/sidebar.tsx` — Server component (wraps client parts):
   - Fixed sidebar: 240px width, full viewport height, secondary background (#1e293b)
   - Top section: "Turnkey" in Heading typography (20px, semibold), 24px top padding
   - Middle section: `<SidebarNav />` component
   - Bottom section: shadcn Separator, then user email (Body muted, #94a3b8), then `<LogoutButton />`
   - Accepts `userEmail` prop to display in footer
   - Sidebar section gap: 32px (xl spacing token)

5. Create `src/app/(authenticated)/layout.tsx`:
   - Server component that calls `requireAuth()` to get session
   - Renders sidebar (passing `session.user.email`) + main content area
   - Main content: fills remaining width, dominant background (#0f172a), 32px top padding (xl)
   - Structure:
     ```tsx
     <div className="flex h-screen">
       <Sidebar userEmail={session.user.email} />
       <main className="flex-1 overflow-y-auto bg-[#0f172a] pt-8 px-8">
         {children}
       </main>
     </div>
     ```
  </action>
  <verify>
    <automated>cd "C:\Users\mattg\OneDrive\Documents\Revel 1 Solutions\AI Agents\Workspaces\DLauch AppFolio" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sidebar.tsx contains text "Turnkey"
    - src/components/sidebar.tsx contains "240" (width)
    - src/components/sidebar.tsx contains "#1e293b" or "slate-800" (secondary background)
    - src/components/sidebar-nav.tsx contains "usePathname" and "Dashboard"
    - src/components/sidebar-nav.tsx contains "#3b82f6" or "blue-500" (accent for active)
    - src/components/logout-button.tsx contains `signOut` and "Log Out"
    - src/components/logout-button.tsx contains "#ef4444" or "red-500" (destructive color)
    - src/app/(authenticated)/layout.tsx contains `requireAuth` and `Sidebar`
    - src/lib/auth-helpers.ts contains `auth()` and `redirect("/login")`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Authenticated layout renders sidebar (240px, secondary bg) with product name, nav items, user email, and logout button. All interaction states per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Build dashboard page and Unit Turn placeholder</name>
  <files>
    src/app/(authenticated)/dashboard/page.tsx,
    src/app/(authenticated)/unit-turn/page.tsx,
    src/app/page.tsx
  </files>
  <read_first>
    src/app/(authenticated)/layout.tsx
    src/components/sidebar-nav.tsx
    .planning/phases/01-foundation-appfolio-connectivity/01-UI-SPEC.md
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
  </read_first>
  <action>
1. Update `src/app/page.tsx` (root route) to redirect to `/dashboard`:
   ```typescript
   import { redirect } from "next/navigation";
   export default function Home() { redirect("/dashboard"); }
   ```

2. Create `src/app/(authenticated)/dashboard/page.tsx`:
   - Page heading: "Dashboard" in Heading typography (20px, semibold, #f8fafc)
   - AppFolio connectivity status card (placeholder — will be wired in Plan 04):
     - shadcn Card with secondary background (#1e293b), 1px solid #334155 border, 8px radius
     - Card title: "AppFolio Connection" in Label typography (14px, semibold)
     - Card body: "Checking AppFolio connection..." in Body muted (#94a3b8) — empty state per UI-SPEC
     - Will later show Badge with "Connected to sandbox" or "Connection failed"
     - Card padding: 16px (md)
   - Unit Turn nav card:
     - shadcn Card with secondary background, same border/radius as above
     - Card title: "Unit Turn" in Label typography
     - Card body: "Create and manage unit turn work orders." in Body text (#f8fafc)
     - Card is clickable — wraps in Next.js Link to `/unit-turn`
     - Hover state: border brightens to slate-500
     - Card-to-card gap: 24px (lg spacing token)
   - Cards arranged in a responsive grid: 2 columns on lg screens, 1 column on smaller

3. Create `src/app/(authenticated)/unit-turn/page.tsx`:
   - Placeholder page showing "Unit Turn" heading
   - Body text: "Unit turn automation will be available in a future update."
   - This satisfies PORTAL-04 — the nav entry point exists and routes somewhere
  </action>
  <verify>
    <automated>cd "C:\Users\mattg\OneDrive\Documents\Revel 1 Solutions\AI Agents\Workspaces\DLauch AppFolio" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains `redirect("/dashboard")`
    - src/app/(authenticated)/dashboard/page.tsx contains text "Dashboard" as heading
    - src/app/(authenticated)/dashboard/page.tsx contains text "AppFolio Connection"
    - src/app/(authenticated)/dashboard/page.tsx contains text "Checking AppFolio connection..."
    - src/app/(authenticated)/dashboard/page.tsx contains text "Unit Turn"
    - src/app/(authenticated)/dashboard/page.tsx contains text "Create and manage unit turn work orders."
    - src/app/(authenticated)/dashboard/page.tsx contains Link to "/unit-turn"
    - src/app/(authenticated)/unit-turn/page.tsx contains text "Unit Turn"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Dashboard page renders with AppFolio status card (placeholder), Unit Turn nav card linking to /unit-turn, and Unit Turn placeholder page. Root route redirects to /dashboard. PORTAL-01 and PORTAL-04 satisfied.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Authenticated layout has 240px sidebar with "Turnkey" branding
3. Dashboard shows "AppFolio Connection" card and "Unit Turn" card
4. Unit Turn card links to /unit-turn page
5. Logout button uses destructive color and calls signOut
6. Active nav item shows accent left border
</verification>

<success_criteria>
- Portal shell matches UI-SPEC: sidebar (240px, #1e293b), dashboard cards, interaction states
- Navigation from dashboard to /unit-turn works
- Logout immediately invalidates session
- All copywriting matches UI-SPEC contract exactly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-appfolio-connectivity/01-02-SUMMARY.md`
</output>
